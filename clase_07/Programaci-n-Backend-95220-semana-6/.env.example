PORT=8080

# Connection string de MongoDB Atlas. Reemplazá usuario, password y
# cluster por los tuyos, y agregá el nombre de la base al final
# (booking_system). NUNCA commitees el .env real con la password.
MONGO_URI=mongodb://localhost:27017/booking_system?retryWrites=true&w=majority
